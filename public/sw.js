const CACHE = "1pm-club-static-v2";
const ASSETS = ["/images/logo.png", "/images/switchlogo.png"];
const offlinePage = "<!doctype html><html lang=\"en\"><meta charset=\"utf-8\"><meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"><title>You’re offline · The 1PM Club</title><body style=\"margin:0;padding:3rem;background:#F7F7F7;color:#000728;font-family:Arial,sans-serif\"><h1 style=\"color:#0224CC\">You’re offline.</h1><p>Reconnect to load The 1PM Club. Your cart remains stored on this device.</p></body></html>";
self.addEventListener("install", (event) => event.waitUntil(caches.open(CACHE).then((cache) => cache.addAll(ASSETS)).then(() => self.skipWaiting())));
self.addEventListener("activate", (event) => event.waitUntil(caches.keys().then((keys) => Promise.all(keys.filter((key) => key !== CACHE).map((key) => caches.delete(key)))).then(() => self.clients.claim())));
self.addEventListener("fetch", (event) => { const { request } = event; if (request.method !== "GET" || request.url.includes("supabase.co")) return; const url = new URL(request.url); if (url.origin !== self.location.origin) return; if (request.destination === "image") { event.respondWith(caches.match(request).then((cached) => cached || fetch(request))); return; } event.respondWith(fetch(request).catch(() => new Response(offlinePage, { headers: { "Content-Type": "text/html; charset=utf-8" } }))); });
